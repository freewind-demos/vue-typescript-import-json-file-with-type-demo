Vue Typescript Import Json File Demo
====================================

Import a json file and display its data.

```
npm install
npm run serve
```

Notice
------

- should define a file like `shims-json.d.ts`
- `tsconfig.json` should have `includes: ["src/**/*.json"]`
